# Supabase Postgres — copy from Project Settings → Database → Connection string (URI).
# Replace [YOUR-PASSWORD] with your database password (from Database settings), not the anon key.
#
# For SQLAlchemy + psycopg3:
# DATABASE_URL=postgresql+psycopg://postgres:[YOUR-PASSWORD]@db.<project-ref>.supabase.co:5432/postgres?sslmode=require
#
DATABASE_URL=postgresql+psycopg://postgres:[YOUR-PASSWORD]@db.gzvtixylgfqkvoldmvtf.supabase.co:5432/postgres?sslmode=require

# API (Phase 1+)
CORS_ORIGINS=https://solvia.app,http://localhost:8000
